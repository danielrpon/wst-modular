from .exito import config as exito_config
from .farmatodo import config as farmatodo_config
from .olimpica import config as olimpica_config
from .tiendas_d1 import config as d1_config
from .zara import config as zara_config
from .tiendas_jumbo import config as jumbo_config
from .mercadolibre import config as mercadolibre_config
from .inkafarma import config as inkafarma_config
all_configurations = [
    zara_config,
    # exito_config,
    # farmatodo_config,
    # olimpica_config,
    # d1_config,
    # jumbo_config,
    # inkafarma_config,
    # mercadolibre_config
]
